import 'package:flutter/material.dart';

class DrawMenuLogout extends StatelessWidget {
  const DrawMenuLogout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('drawmenulogout')
        ],
      ),
    );
  }
}
