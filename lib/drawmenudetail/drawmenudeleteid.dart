import 'package:flutter/material.dart';

class DrawMenuDeleteId extends StatelessWidget {
  const DrawMenuDeleteId({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('회원탈퇴'),
    );
  }
}
